# --- Веб-сервер и фреймворк ---
fastapi==0.109.2
uvicorn[standard]==0.27.1  # [standard] включает uvloop, httptools, websockets для лучшей производительности

# --- Yandex Cloud SDK и его ключевые зависимости ---

protobuf==5.29.4         # Явно указываем из-за частых конфликтов версий
requests==2.32.3         # Для HTTP-запросов, часто используется SDK

# --- Валидация данных (используется FastAPI и вашими моделями) ---
pydantic==2.11.2

# --- Утилиты, используемые в коде ---
tqdm==4.67.1             

# --- Зависимости, которые могут потребоваться для async операций или SDK ---
aiohttp==3.9.3         
aiofiles==24.1.0    
jinja2==3.1.6
