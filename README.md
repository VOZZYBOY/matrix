# MatrixAI - Clinic Assistant API (Версия 4.3.1 — Multi-Tenant & Multi-Chain, Multi-Worker)

Современный API для многопользовательского (мультитенантного) ИИ-ассистента медицинских клиник. Использует передовые технологии: RAG (Retrieval-Augmented Generation), ReAct-подобный агентский цикл, интеграцию с внешними API клиник и **интеллектуальную систему определения завершенности сообщений** для улучшенного пользовательского опыта.

## Описание

MatrixAI представляет собой высокопроизводительное FastAPI-приложение, которое предоставляет комплексный API для взаимодействия с ИИ-ассистентом медицинских клиник. Система построена с использованием современного стека технологий:

### Основные технологии:
-   **OpenAI GPT-4.1** в качестве основной языковой модели (LLM)
-   **OpenAI o3-mini** для анализа завершенности сообщений с structured output
-   **GigaChat Embeddings** для создания векторных представлений документов
-   **ChromaDB** как векторная база данных для RAG с изолированными коллекциями для каждой клиники
-   **BM25 Retriever** + ChromaDB (Ensemble Retriever) для гибридного поиска
-   **Redis** для хранения истории диалогов с TTL=1 час и изоляцией по тенанту/пользователю
-   **Новый унифицированный API** (`/api/v1/AI/getMultipleData`) для интеграции с внешними системами клиник

### Архитектурные особенности:
-   **ReAct-агентский цикл:** Многошаговое выполнение задач с динамическим вызовом инструментов
-   **Мигрированные API функции:** 6 ключевых функций теперь используют новый API endpoint
-   **Интеллектуальная система завершенности сообщений:** Предотвращение ответов на неполные сообщения
-   **Система диалогов с временным хранением:** Автоматическая очистка истории через 1 час
-   **Продвинутые инструменты:** Поиск врачей, проверка услуг, запись на прием, получение расписания
-   **Управление конфигурациями тенантов:** Гибкая настройка для каждой клиники

## Ключевые возможности

### 🏥 Медицинские функции
-   **Умный поиск специалистов:** Поиск врачей по услугам, специализациям и филиалам
-   **Управление записями:** Полноценная система записи на прием с проверкой доступности
-   **Проверка услуг:** Верификация наличия услуг в конкретных филиалах
-   **Ценовые сравнения:** Сопоставление стоимости услуг между филиалами

### 🤖 ИИ-возможности  
-   **RAG-система:** Интеллектуальный поиск в базе знаний с контекстуальными ответами
-   **ReAct-агент:** Многоэтапное решение задач с автоматическим выбором инструментов
-   **Умная обработка сообщений:** Система определения завершенности предотвращает ответы на неполные вопросы
-   **Накопление фрагментов:** Автоматическое склеивание частичных сообщений до получения полной мысли
-   **Контекстная память:** Сохранение истории диалогов с автоочисткой через 1 час
-   **Адаптивные промпты:** Настройка поведения ассистента для каждой клиники

### 🔧 Технические возможности
-   **Мультитенантность + Multi-Chain:** Полная изоляция данных между клиниками и их филиальными сетями (tenant_id + chain_id)
-   **API интеграция:** Унифицированный endpoint `/api/v1/AI/getMultipleData` для внешних систем
-   **Мигрированные функции:** 6 ключевых функций переведены на новый API
-   **Гибридный поиск:** Комбинация векторного и BM25 поиска
-   **Масштабируемость:** Готовность к горизонтальному масштабированию

### 📊 Административные возможности
-   **Управление конфигурациями:** API для настройки каждого тенанта
-   **Мониторинг:** Детальное логирование и health-check эндпоинты
-   **Веб-интерфейс:** Простой UI для тестирования и администрирования

## Архитектура системы

### Основные компоненты

| Файл | Назначение | Ключевые функции |
|------|------------|------------------|
| `app.py` | FastAPI приложение | REST API endpoints, зависимости, веб-интерфейс |
| `matrixai.py` | Ядро ИИ-ассистента | ReAct-цикл, LangChain интеграция, управление инструментами |
| `message_completeness_analyzer.py` | **Анализ завершенности** | **Интеллектуальное определение неполных сообщений, накопление фрагментов** |
| `clinic_functions.py` | Бизнес-логика клиники | **6 мигрированных функций** на новый API, медицинские операции |
| `rag_setup.py` | RAG-инфраструктура | Инициализация ChromaDB, GigaChat, BM25, обработка данных |
| `redis_history.py` | Управление историей | **TTL=1 час**, изоляция по тенанту/пользователю |
| `client_data_service.py` | API интеграция | Взаимодействие с **новым унифицированным API** клиник |

### Мигрированные API функции (Новый API)
Следующие 6 функций теперь используют унифицированный endpoint `/api/v1/AI/getMultipleData`:

1. **`FindEmployees`** - Поиск сотрудников с поддержкой всех режимов API
2. **`CheckServiceInFilial`** - Проверка доступности услуги в филиале  
3. **`FindServiceLocations`** - Поиск всех филиалов с определенной услугой
4. **`ListServicesInFilial`** - Список всех услуг в конкретном филиале
5. **`GetEmployeeServices`** - Получение услуг конкретного сотрудника
6. **`ListEmployeeFilials`** - Список филиалов, где работает сотрудник

### Структура директорий
```
matrixai/
├── 📁 base/                    # Основные данные тенантов (JSON)
├── 📁 tenant_configs/          # Конфигурации клиник  
├── 📁 chroma_db_clinic_giga/   # Векторная база данных
├── 📁 static/, templates/      # Веб-интерфейс
├── 📁 __pycache__/            # Python кэш
├── 🐳 docker-compose.yml       # Контейнеризация
├── 📄 requirements.txt         # Python зависимости
└── 📋 api.log                 # Логи системы
```

## Установка и Запуск

## Быстрый старт

### 🐳 Запуск с Docker Compose (Рекомендуется)

1. **Клонируйте репозиторий:**
   ```bash
   git clone <repository_url>
   cd matrixai
   ```

2. **Настройте переменные окружения:**
   ```bash
   cp .env.example .env
   # Отредактируйте .env файл с вашими ключами API
   ```

3. **Запустите систему:**
   ```bash
   docker-compose up --build -d
   ```

4. **Проверьте работоспособность:**
   ```bash
   curl http://localhost:8001/health
   ```

✅ Система доступна по адресу: `http://localhost:8001`

### 💻 Локальная установка

```bash
# Создание виртуального окружения
python -m venv .venv
source .venv/bin/activate  # Linux/macOS
# .venv\Scripts\activate   # Windows

# Установка зависимостей
pip install -r requirements.txt

# Запуск приложения
uvicorn app:app --reload --host 0.0.0.0 --port 8001
```

## Конфигурация переменных окружения

Создайте файл `.env` в корневой директории:

```dotenv
# === ОСНОВНЫЕ API КЛЮЧИ ===
OPENAI_API_KEY="sk-proj-..."                    # OpenAI API для GPT-4 и o3-mini
GIGACHAT_CREDENTIALS="<токен_GigaChat>"         # GigaChat для embeddings

# === МОДЕЛИ ===
GIGA_EMBEDDING_MODEL="EmbeddingsGigaR"
GIGA_SCOPE="GIGACHAT_API_PERS" 
GIGA_VERIFY_SSL="False"

# === АНАЛИЗ ЗАВЕРШЕННОСТИ СООБЩЕНИЙ ===
MESSAGE_COMPLETENESS_MODEL="o3-mini"            # Модель для анализа завершенности
MESSAGE_COMPLETENESS_ENABLED="true"             # Включить/выключить систему
MESSAGE_ANALYSIS_CACHE_TTL="60"                 # TTL кэша анализов в секундах
MESSAGE_ACCUMULATOR_TTL="300"                   # TTL накопителей в секундах

# === REDIS КОНФИГУРАЦИЯ ===
REDIS_HOST="localhost"
REDIS_PORT="6379"
REDIS_DB="0"
REDIS_PASSWORD=""                               # Опционально

# === RAG НАСТРОЙКИ ===
BASE_DATA_DIR="base"                           # Директория с данными клиник
CHROMA_PERSIST_DIR="chroma_db_clinic_giga"     # ChromaDB база
CHUNK_SIZE=1000                                # Размер чанков для RAG
CHUNK_OVERLAP=200                              # Перекрытие чанков
SEARCH_K=5                                     # Количество результатов поиска

# === ПРИЛОЖЕНИЕ ===
APP_HOST="0.0.0.0"
APP_PORT=8001
APP_LOG_LEVEL="info"
```

## API Эндпоинты

### 🔄 Основные эндпоинты

| Метод | URL | Описание | Требования |
|-------|-----|----------|------------|
| `POST` | `/ask` | **Главный чат API** | `tenant_id`, Bearer-токен (HTTP `Authorization` header) <br/>`client_api_token` — устаревший fallback |
| `GET` | `/health` | Проверка статуса системы | - |
| `GET` | `/` | Веб-интерфейс | - |

### 👥 Управление историей

| Метод | URL | Описание |
|-------|-----|----------|
| `GET` | `/history/{tenant_id}/{user_id}` | Получение истории чата |
| `POST` | `/clear_history` | Очистка истории (TTL=1 час) |

### ⚙️ Управление тенантами

| Метод | URL | Описание |
|-------|-----|----------|
| `POST` | `/tenant_settings` | Настройка конфигурации клиники (теперь требует `tenant_id` **и** `chain_id`) |
| `GET` | `/tenant_settings/{tenant_id}/{chain_id}` | Получение настроек для конкретной цепочки |
| `GET` | `/tenants` | Список всех тенантов |

### 📊 Мониторинг

| Метод | URL | Описание |
|-------|-----|----------|
| `GET` | `/logs` | Последние логи системы |
| `GET` | `/message_analysis_stats` | Статистика анализа завершенности |

## Пример использования API

### Отправка сообщения ассистенту

```bash
curl -X POST "http://localhost:8001/ask" \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer your_api_token" \
  -d '{
    "message": "хочу записаться к врачу кардиологу",
    "tenant_id": "medyumed.2023-04-24",
    "phone_number": "+7900123456"
  }'

> Если вы не можете добавить заголовок, передайте `client_api_token` в теле запроса — бэкенд примет его как fallback.
```

**Примечание:** Если сообщение определяется как неполное (например, только "хочу записаться к"), система вернет пустой ответ и будет ждать продолжения.

### Получение истории чата

```bash
curl "http://localhost:8001/history/medyumed.2023-04-24/user123"
```

### Настройка конфигурации клиники

```bash
curl -X POST "http://localhost:8001/tenant_settings" \
  -H "Content-Type: application/json" \
  -d '{
    "tenant_id": "medyumed.2023-04-24",
    "chain_id": "default",
    "prompt_addition": "Всегда упоминайте о скидках для пенсионеров",
    "clinic_info_docs": ["Описание клиники", "Правила записи"]
  }'
```

## Структура данных клиник

### Формат данных в `base/*.json`

Каждый JSON-файл должен содержать массив объектов со следующими полями:

```json
[
  {
    "serviceId": "1",
    "serviceName": "Консультация кардиолога", 
    "serviceDescription": "Подробная консультация...",
    "categoryId": "1",
    "categoryName": "Кардиология",
    "employeeId": "101",
    "employeeFullName": "Иванов Иван Иванович",
    "employeeDescription": "Кардиолог высшей категории...",
    "filialId": "1", 
    "filialName": "Центральная клиника",
    "price": "2500"
  }
]
```

### Обязательные поля для корректной работы

| Поле | Назначение | Пример |
|------|------------|--------|
| `serviceId`, `serviceName` | Идентификация услуг | `"1"`, `"Консультация кардиолога"` |
| `employeeId`, `employeeFullName` | Данные врачей | `"101"`, `"Иванов И.И."` |
| `filialId`, `filialName` | Информация о филиалах | `"1"`, `"Центральная клиника"` |
| `categoryName` | Медицинские специальности | `"Кардиология"` |
| `price` | Стоимость услуг | `"2500"` |

## Новые возможности (v4.3)

### 🆕 Обновления версии 4.3

* **Bearer-токен в заголовке Authorization** — основной способ авторизации; `client_api_token` остаётся как fallback.
* **Поддержка нескольких Uvicorn worker-процессов** (по умолчанию 2) для лучшей параллельности и отсутствия блокировок при первой загрузке данных.
* **Авто-распаковка обёртки `services`** в JSON-ответах при скачивании данных (исправляет ошибку `'str' object has no attribute 'get'`).
* **Автоматическая загрузка и кэширование данных**: при первом запросе нового `tenant_id + chain_id` сервис скачивает все страницы с внешнего API, сохраняет в `base/<tenant>/<tenant>_<chain>.json` и переиспользует файл при следующих стартах.

---

### 🧠 Интеллектуальная система определения завершенности сообщений

**Революционная функция:** Система анализирует каждое входящее сообщение и определяет, завершена ли мысль пользователя или требуется ждать продолжения.

#### Ключевые особенности:
- **LLM-анализ:** Использует OpenAI o3-mini для семантического понимания завершенности
- **Накопление фрагментов:** Автоматически склеивает части сообщений до получения полной мысли
- **Умные разделители:** Контекстно-aware склейка с правильной пунктуацией
- **Медицински-специфичные промпты:** Оптимизировано для естественной речи пациентов
- **Graceful degradation:** При ошибках анализа предпочитает отвечать, а не заставлять ждать

#### Пример работы:
```
Пользователь: "хочу записаться к"        → Статус: INCOMPLETE, ждем продолжения
Пользователь: "врачу кардиологу"         → Статус: COMPLETE, обрабатываем: "хочу записаться к врачу кардиологу"
```

#### Технические детали:
- **Structured Output:** Использует Pydantic-модели для надежного парсинга ответов LLM
- **Кэширование:** 60-секундный TTL для анализов в памяти для оптимизации производительности
- **Эвристики:** Быстрые проверки для очевидных случаев (пустые сообщения, короткие фразы)
- **Fallback-стратегия:** При ошибках возвращает `COMPLETE` для лучшего UX

### 🚀 Мигрированные API функции (v4.0)

**6 ключевых функций** теперь используют унифицированный API endpoint `/api/v1/AI/getMultipleData`:

1. **FindEmployees** - Расширенный поиск сотрудников
2. **CheckServiceInFilial** - Проверка услуг в филиалах  
3. **FindServiceLocations** - Геолокация услуг
4. **ListServicesInFilial** - Каталог услуг
5. **GetEmployeeServices** - Профили врачей
6. **ListEmployeeFilials** - География работы врачей

### ⏰ Оптимизированное управление историей

- **TTL=1 час:** Автоматическая очистка диалогов для оптимизации памяти
- **Изоляция данных:** Полное разделение истории между клиниками
- **Redis оптимизация:** Эффективное использование памяти

### 🔧 Расширенные инструменты

- Интеллектуальный поиск специалистов по критериям
- Динамическая проверка доступности услуг
- Автоматическое сопоставление цен между филиалами
- Упрощенная система записи на прием

## Мониторинг и отладка

### 📊 Health Check

```bash
curl http://localhost:8001/health
```

Ответ содержит статус всех компонентов системы:
- ✅ Инициализация RAG
- ✅ Подключение к Redis  
- ✅ Загрузка моделей
- ✅ Готовность API

### 📋 Логирование

Система ведет детальные логи в файле `api.log`:

```bash
# Просмотр последних логов через API
curl http://localhost:8001/logs

# Или напрямую из файла
tail -f api.log
```

### 🧠 Отладка системы завершенности сообщений

Для мониторинга работы анализа завершенности:

```bash
# Статистика анализа сообщений
curl http://localhost:8001/message_analysis_stats

# Логи содержат информацию о каждом анализе:
# - Статус завершенности (COMPLETE/INCOMPLETE)
# - Уверенность модели (0.0-1.0)
# - Время анализа
# - Количество накопленных частей сообщения
```

### 🔍 Отладка ReAct-цикла

Включите debug-логирование для подробной информации о работе агента:

```python
# В matrixai.py уже настроено логирование каждой итерации
# Проверяйте логи для понимания принятия решений ИИ
```

## Требования системы

### Минимальные требования
- **Python:** 3.10+
- **RAM:** 4GB
- **Диск:** 2GB свободного места
- **Redis:** любая совместимая версия

### Рекомендуемые требования  
- **Python:** 3.11+
- **RAM:** 8GB+
- **Диск:** 10GB+ (для больших баз знаний)
- **CPU:** 4+ ядер
- **Redis:** 6.0+

## Развертывание в production

### 🐳 Docker Production

```bash
# Создание production образа (образ уже содержит запуск Uvicorn с **2 worker-процессами**) 
docker build -t matrixai:production .

# Запуск с production настройками
docker run -d \
  --name matrixai-prod \
  -p 8001:8001 \
  -v $(pwd)/.env:/app/.env \
  -v $(pwd)/base:/app/base \
  -v $(pwd)/tenant_configs:/app/tenant_configs \
  matrixai:production
```

### ⚡ Nginx конфигурация

```nginx
server {
    listen 80;
    server_name your-domain.com;
    
    location / {
        proxy_pass http://localhost:8001;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}
```

## Поддержка и разработка

### 🤝 Участие в разработке

1. Fork репозитория
2. Создайте feature branch
3. Внесите изменения с тестами
4. Создайте Pull Request

### 📚 Документация API

После запуска приложения интерактивная документация доступна по адресам:
- **Swagger UI:** `http://localhost:8001/docs`
- **ReDoc:** `http://localhost:8001/redoc`

### 🐛 Сообщения об ошибках

При обнаружении проблем создайте issue с:
- Описанием проблемы
- Логами из `api.log`
- Шагами для воспроизведения
- Версией системы

---

## 📝 История изменений

### Версия 4.3.1 (Текущая)
**Дата:** 24 июня 2025  
**Исправления:**
- 🐛 **Критическое исправление:** Устранена ошибка `UnboundLocalError: cannot access local variable 'history_messages'` в функции `run_agent_like_chain`
- ✅ **Техническая оптимизация:** Переменная `history_messages` перенесена в начало функции для корректного доступа во всех блоках кода
- 🔧 **Улучшение стабильности:** Исправлена обработка технических подтверждений от администратора
- 📋 **Обновлена документация:** Добавлен раздел истории изменений в README

**Затронутые файлы:**
- `matrixai.py` - основное исправление логики обработки истории сообщений
- `README.md` - обновление документации

**Важность:** Критическое исправление для стабильной работы системы подтверждений действий.

### Версия 4.3.0
**Дата:** Июнь 2025  
**Основные изменения:**
- 🚀 **Multi-Chain архитектура:** Поддержка филиальных сетей (tenant_id + chain_id)
- 🤖 **Интеллектуальная система завершенности сообщений:** Предотвращение ответов на неполные сообщения
- 🔄 **6 мигрированных API функций:** Переход на унифицированный endpoint `/api/v1/AI/getMultipleData`
- ⏰ **TTL для истории диалогов:** Автоматическая очистка через 1 час
- 🎯 **Улучшенный ReAct-цикл:** Оптимизация многошагового выполнения задач

---

## Лицензия

Проект распространяется под **MIT License**.

---

**MatrixAI v4.3.1** - Современное решение для автоматизации медицинских клиник с использованием передовых технологий искусственного интеллекта и интеллектуальной обработкой фрагментированных сообщений.
